import { RotatingRings } from "./rotating-rings";

export const metadata = {
  title: "Rotating Rings",
  description: "An interactive rigid-body dynamics experiment inspired by an International Physicists’ Tournament problem.",
};

export default function RotatingRingsPage() {
  return <RotatingRings />;
}
